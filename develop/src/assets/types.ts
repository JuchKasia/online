export type Product = {
    id: number,
    title: string,
    brand: string,
    category: string,
    description: string,
    price: number,
    discount: number,
    stock: number,
    size: string,
    color: string,
    images: [string]
}

